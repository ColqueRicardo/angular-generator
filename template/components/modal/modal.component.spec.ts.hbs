import { ComponentFixture, TestBed } from '@angular/core/testing';

import { {{entityName}}ModalComponent } from './modal.component';

describe('{{entityName}}ModalComponent', () => {
  let component: {{entityName}}ModalComponent;
  let fixture: ComponentFixture<{{entityName}}ModalComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [{{entityName}}ModalComponent]
    });
    fixture = TestBed.createComponent({{entityName}}ModalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
