import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { ErrorService } from '../service-error/error-service.service';
import { CommonService } from '../common-service/CommonService';
import { {{entityName}}Model } from '../../model/{{entityName.toLowerCase()}}.model';

@Injectable({
  providedIn: 'any'
})
export class {{entityName}}Service extends CommonService<{{entityName}}Model> {
  override url = '{{entityName.toLowerCase()}}';
  override model = {{entityName}}Model;
  constructor(httpClient: HttpClient, errorService: ErrorService) {
    super(httpClient, errorService);
  }
}
