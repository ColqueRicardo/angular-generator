import { Component, Inject } from '@angular/core';
import { FormBuilder } from '@angular/forms';
import { MAT_DIALOG_DATA, MatDialogRef } from '@angular/material/dialog';
import { ModalComponent } from '../../../../core/class/modal-class';
import { {{entityName}}Model } from '../../../../core/model/{{entityNameLowerCase}}.model';
import { {{entityName}}Service } from '../../../../core/service/{{entityNameLowerCase}}/{{entityNameLowerCase}}.service';
import { UntilDestroy, untilDestroyed } from '@ngneat/until-destroy';

@UntilDestroy()
@Component({
  selector: 'app-{{entityNameLowerCase}}-modal',
  templateUrl: './{{entityNameLowerCase}}-modal.component.html',
  styleUrls: ['./{{entityNameLowerCase}}-modal.component.scss']
})
export class {{entityName}}ModalComponent extends ModalComponent {
  override model = {{entityName}}Model
  constructor(
    protected override dialogRef: MatDialogRef<any>,
    @Inject(MAT_DIALOG_DATA) public override dt: any,
    protected override service: {{entityName}}Service,
    private fb: FormBuilder,
  ) {
    super(dialogRef, dt, service)
    this.formGroup = this.fb.group({
      id: [null],
    })
  }
}
