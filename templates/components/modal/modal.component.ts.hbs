import { Component, Inject } from '@angular/core';
import { FormBuilder } from '@angular/forms';
import { MAT_DIALOG_DATA, MatDialogRef } from '@angular/material/dialog';
import { ModalComponent } from '../../../../../core/class/modal-class';
import { {{className}}Model } from '../../../../../core/model/{{kebabName}}.model';
import { {{className}}Service } from '../../../../../core/service/{{kebabName}}/{{kebabName}}.service';
import { UntilDestroy } from '@ngneat/until-destroy';

@UntilDestroy()
@Component({
  selector: 'app-{{kebabName}}-modal',
  templateUrl: './{{kebabName}}-modal.component.html',
  styleUrls: ['./{{kebabName}}-modal.component.scss']
})
export class {{className}}ModalComponent extends ModalComponent {
  override model = {{className}}Model
  constructor(
    protected override dialogRef: MatDialogRef<any>,
    @Inject(MAT_DIALOG_DATA) public override dt: any,
    protected override service: {{className}}Service,
    private fb: FormBuilder,
  ) {
    super(dialogRef, dt, service)
    this.formGroup = this.fb.group({
      id: [null],
    })
  }
}
