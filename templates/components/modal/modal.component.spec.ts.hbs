import { ComponentFixture, TestBed } from '@angular/core/testing';

import { {{className}}ModalComponent } from './{{kebabClass}}-modal.component';

describe('{{className}}ModalComponent', () => {
  let component: {{className}}ModalComponent;
  let fixture: ComponentFixture<{{className}}ModalComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [{{className}}ModalComponent]
    });
    fixture = TestBed.createComponent({{className}}ModalComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
