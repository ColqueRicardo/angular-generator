import { Component } from '@angular/core';
import { MatDialog } from '@angular/material/dialog';
import { {{entityName}}ModalComponent } from './modal/modal.component';
import { {{entityName}}Service } from '../../../core/service/{{entityName.toLowerCase()}}/{{entityName.toLowerCase()}}.service';
import { ListPageComponent } from '../../../core/class/list-page-component';

@Component({
  selector: 'app-{{entityName.toLowerCase()}}s',
  templateUrl: './{{entityName.toLowerCase()}}s.component.html',
  styleUrls: ['./{{entityName.toLowerCase()}}s.component.scss'],
})
export class {{entityName}}sComponent extends ListPageComponent {

  override displayedColumns = [
    'id',
    'actions'
  ]

  override modal = {{entityName}}ModalComponent

  constructor(
    private {{entityName.toLowerCase()}}Service: {{entityName}}Service,
    private matDialog: MatDialog,
  ) {
    super({{entityName.toLowerCase()}}Service, matDialog)
  }
}
